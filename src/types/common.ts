export type Filter = {
  page: number;
  order: string;
  sort: string;
  search: string;
};

export type OSFilter = {
  page: number;
  order: string;
  sort: string;
  search: string;
  select: string;
};
